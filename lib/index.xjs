defmodule XJS.Test do
  import XJS

  def run() do
    xjs do
      con obj = %{
        0 => 0,
        1 => 1,
        "c": 2 
      }
      let log = console.log
      let fun = fn x, y ->
        let z = x + y
      end
      log fun(0, 1)
      "test"
      console.log 
      console.log 0
      a.b.c
    end
  end
end
